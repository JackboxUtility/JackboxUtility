class WindowInformation {
  bool maximized;
  int width;
  int height;
  int x;
  int y;

  WindowInformation(
      {this.maximized = false, this.width = 1300, this.height = 750, this.x=10, this.y=10});
}
