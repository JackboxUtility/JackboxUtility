class WindowInformation {
  bool maximized;
  int width;
  int height;
  int x;
  int y;

  WindowInformation(
      {this.maximized = false, this.width = 1400, this.height = 800, this.x=10, this.y=10});
}
